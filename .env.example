# Copy this file to .env and fill in values. Never commit .env.
# See lib/core/constants/env_keys.dart for all keys consumed by the app.

# App
APP_ENV=development

# Encryption key for sensitive field encryption at rest (min 32 chars)
DB_ENCRYPTION_KEY=
